import { RtvReturnConsoleV2 } from "@/components/RtvReturnConsoleV2";import { PageHeader } from "@/components/PageHeader";import { getCurrentOrgContext } from "@/lib/org";import { createClient } from "@/lib/supabase/server";
export default async function RtvReturnsPage(){const c=await getCurrentOrgContext();if(!c?.org)return null;const s=createClient();const{data}=await s.from("purchase_orders").select("id,po_number,supplier_name,status,purchase_order_items(id,product_id,location_id,quantity_received,products(sku,name),locations(location_code,display_code))").eq("org_id",c.org.id).in("status",["RECEIVED","PARTIALLY_RECEIVED","COMPLETED"]).order("created_at",{ascending:false}).limit(200);const purchaseOrders=(data??[]).filter((order:any)=>(order.purchase_order_items??[]).some((line:any)=>line.quantity_received>0));return <div><PageHeader title="Vendor Returns (RTV)" subtitle="Return accepted PO stock from its original putaway bin with full traceability"/><div className="p-8"><RtvReturnConsoleV2 orgId={c.org.id} purchaseOrders={purchaseOrders as any}/></div></div>}
