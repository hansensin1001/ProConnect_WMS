// Keep temporarily disabled modules inaccessible even if a user opens a saved
// URL directly. Re-enable by changing this flag and restoring its navigation.
export const SCAN_MODULE_ENABLED = false;
